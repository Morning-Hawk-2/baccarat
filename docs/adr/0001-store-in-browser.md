# 記録はブラウザの中にだけ保存する

「ブラウザを閉じても記録が残ってほしい」に対して、サーバーに送って残す案と、ブラウザの中に保存する案があった。後半の実装時間ではサーバーもログインも用意できず、使うのは自分ひとりでPCのブラウザ1つと決まっているので、ブラウザの中にだけ保存し、サーバーには送らないことにした。この決定により、他の端末との同期・共有は「今回やらないこと」になる。保存が使えない環境（プライベートウィンドウなど）では、記録が残らないことを画面に出す。
