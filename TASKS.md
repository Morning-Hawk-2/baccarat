# TASKS

上から順に実装する。各タスクは15分以内を目安とし、途中で止まっても直前のタスクまでは動く状態を保つ。

## 1. Vite+React+TSの雛形をセットアップする
完了定義: `npm run dev` を実行したブラウザに「baccarat」という見出しだけが表示される。

## 2. バカラの点数計算ロジックを実装する(カード配列→合計を9で割った下1桁)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、点数計算のテストがPASSと表示される。

## 3. 第三カードルールの判定ロジックを実装する(Player/BankerそれぞれのDraw/Stand判定関数)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、ナチュラルや代表的な手のDraw/Stand判定テストがPASSと表示される。

## 4. 1ハンド分のカード配布ロジックを実装する(毎ハンド新規に52枚から配布、第三カード発生率が70〜80%になるよう再抽選)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、多数ハンドをシミュレートして第三カード発生率が70〜80%に収まることを確認するテストがPASSと表示される。

## 5. 勝敗判定ロジックを実装する(Player win / Banker win / Tie)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、代表的な手の勝敗判定テストがPASSと表示される。

## 6. 「次のハンドへ」ボタンを設置し、配布結果と勝敗をそのまま画面に表示する(回答ステップはまだ無く、結果を見るだけ)
完了定義: 「次のハンドへ」を押すごとに、画面のPlayer/Bankerのカードと勝敗表示がランダムに変わる。

## 7. ベット種別とベット額のランダム選択ロジックを実装し、画面にそのハンドのベット内容を表示する
完了定義: 「次のハンドへ」を押すごとに、画面のベット表示(例: 「今回のベット: Banker $50」)が変わる。

## 8. Player Draw/Standの回答UIを設置し、正解/不正解を即時表示する
完了定義: Playerの初手2枚が表示された状態でDraw/Standのボタンを押すと、画面に正解/不正解が即時表示される。

## 9. Banker Draw/Standの回答UIを設置し、正解/不正解を即時表示する
完了定義: Bankerの初手(および必要な場合はPlayerの3枚目)が表示された状態でDraw/Standのボタンを押すと、画面に正解/不正解が即時表示される。

## 10. 勝敗判定の回答UI(Player win/Banker win/Tieの3択)を設置し、正解/不正解を即時表示する
完了定義: 全カードが開いた状態で3択ボタンのいずれかを押すと、画面に正解/不正解が即時表示される。

## 11. 配当テーブルと4択ダミー生成ロジックを実装し、配当計算の回答UI(4択)を設置する
完了定義: 4択ボタンのいずれかを押すと、画面に正解/不正解が即時表示される。

## 12. 不正解時に正解値と判断根拠を一行表示する機能を追加する(全回答ステップ共通)
完了定義: 意図的に誤答すると、画面に正解の値とその根拠(適用したルールの条件)が一行で表示される。

## 13. 項目別・全体の正答率を累積表示するスコアパネルを追加する
完了定義: 画面にスコアパネルが表示され、回答するたびに該当項目(Player Draw/Stand・Banker Draw/Stand・勝敗判定・配当計算)と全体の正答率が更新される。

## 14. スコアの手動リセットボタン(確認ダイアログ付き)を追加する
完了定義: リセットボタンを押すと確認ダイアログが表示され、OKを押すとスコアパネルの数値がすべて0に戻る。

## 15. 第三カードルール表(カンニングシート)をトグルで表示/非表示できるようにする
完了定義: トグルボタンを押すとルール表が画面に表示され、もう一度押すと隠れる。

## 16. ベット受付・カード配布・支払いの自動演出を実装し、回答ステップ以外は自動進行にする
完了定義: 「次のハンドへ」を押すと、回答不要なステップ(ベット受付→配布→支払い)は自動で進み、回答が必要なステップだけ画面が止まって入力を待つ。

## 17. CSS Modulesでグリーン基調のテーブル風スタイリングとレスポンシブレイアウトを適用する
完了定義: ブラウザ幅をスマホ幅程度まで狭めてもレイアウトが崩れず、緑基調のテーブル風デザインで表示される。

## 18. キーボード操作・ARIA属性・コントラストなどアクセシビリティ対応を行う
完了定義: Tabキーだけで全ての回答ボタン・トグル・リセットボタンを操作でき、フォーカス位置が画面上で視覚的に分かる。

## 19. GitHub Actionsでのビルド・デプロイworkflowとViteのbaseパス設定を行う
完了定義: `https://<user>.github.io/baccarat/` にアクセスするとアプリが表示される。
