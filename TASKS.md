# やること

## MVP(今日やる分)

条件: (1) それだけで「このアプリが何をするものか」が画面で伝わること (2) 入力・処理・表示がつながって、動くものが1本通ること

| # | やること | 完了定義 | 状態 |
|---|---|---|---|
| 1 | HTMLファイルを1つ作り、見出しと空の一覧を表示する | ブラウザで開くと「習慣トラッカー」の見出しと、空の一覧の枠が見える | 未 |
| 2 | 入力欄と「追加」ボタンを置き、押したら一覧に行が増えるようにする | 「30分歩く」と入れて追加を押すと、一覧に「30分歩く」の行が増える | 未 |
| 3 | 各行に今日の分のチェックを置き、押すと入る・もう一度押すと外れるようにする | チェックを押すと印が付き、もう一度押すと消える | 未 |
| 4 | 習慣とチェックの状態をブラウザに保存し、開き直しても残るようにする | タブを閉じて開き直しても、追加した習慣とチェックがそのまま残っている | 未 |

## 持ち帰る分

MVPの条件(画面で何をするものか伝わる/入力→処理→表示が1本通る)には無くても支障が無いもの。完了定義はそのまま残す。

| # | やること | 完了定義 | 今日やらない理由 | 状態 |
|---|---|---|---|---|
| 5 | 一覧の見出しに直近7日分の日付を表示する(チェックの動きはまだ今まで通り「今日1個」のまま) | 一覧の見出しに直近7日分の日付が並んで表示される | 追加→チェック→保存の一本道はMVPの1〜4で通っており、7日表示が無くても支障が無いため | 未 |
| 6 | チェックのデータを「今日1個」から「日付ごと」に持てるように変更し、今日の列だけ引き続き操作できるようにする | 今日の列のチェックを押すと印が付き、もう一度押すと消える(他の日の列は空欄のまま) | 5をやらない今日は、対応するデータ構造の変更も不要なため | 未 |
| 7 | 保存の形式を日付ごとのチェックに対応させ、過去にチェックした日の列にも印を表示する | タブを閉じて開き直しても、今日以外の列にも過去にチェックした印が残って表示される | 6のデータ構造変更が前提のため、6とあわせて後回しにするため | 未 |
| 8 | 今日以外の列は押しても反応しないようにする | 今日の列だけ押すと反応し、他の日の列は押しても印が付かない(グレーアウトなど見た目でも区別できる) | 7日表示(5〜7)自体を今日やらないため、その操作制限も不要なため | 未 |
| 9 | 各行に「削除」ボタンを置き、押すとその習慣が消えるようにする | 削除を押すと行が消え、開き直しても消えたままになっている | 追加→チェック→保存の一本道には無くても支障が無い、取り消し用の機能のため | 未 |
| 10 | 保存が使えない環境のとき、記録が残らないことを画面に出す | プライベートウィンドウで開くと、注意書きが1行表示される | 通常のブラウザで動く一本道の確認には影響しない、例外環境向けの対応のため | 未 |
| 11 | 文字の大きさ・余白・線を整えて読みやすくする | 下記「タスク11の完了判断チェックリスト」の項目にすべて「はい」と答えられる | 画面で機能が伝わる・動く一本道が通ることが優先で、見た目の仕上げは後でよいため | 未 |

### タスク11の完了判断チェックリスト

- [ ] 見出し文字は一覧の項目名より明らかに大きいか？
- [ ] 一覧の行と行の間に隙間があり、上下の行がくっついて見えていないか？
- [ ] チェック済みの日と未チェックの日が、色または記号の違いで見分けられるか？
- [ ] ボタン(追加・削除)は文字と混ざらず、枠や色で「押せる場所」だと分かるか？
- [ ] スマホ幅(375px程度)で文字のはみ出し・重なり・横スクロールが起きていないか？

すべて「はい」ならタスク11は完了。1つでも「いいえ」があれば未完了。

## 今回やらないこと

- 7日より前の履歴を見る — 画面が横に広がるわりに、「続いているか」は7日で十分分かるため
- 連続達成日数（ストリーク）の表示 — 1〜4が動いてからでよい仕上げのため
- 目標回数の設定・リマインド通知 — 通知は「ブラウザだけで動く」範囲を超えるため
- 他の端末との同期・共有 — ログインとサーバーが必要になるため
- 見た目のデザイン（配色・アイコン・アニメーション） — 動くようになってから最後に回すため
