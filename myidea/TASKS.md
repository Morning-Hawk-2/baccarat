# TASKS

上から順に実装する。各タスクは15分以内を目安とし、途中で止まっても直前のタスクまでは動く状態を保つ。

## MVP1(今日やる分)

以下の条件を両方満たす#1〜#20を今日中に実装する。
- 画面だけを見てアプリの用途(バカラディーラーの判断練習)が伝わる
- 回答(入力)→正誤判定(処理)→表示が一本の流れでつながっている

## 1. Vite+React+TSの雛形をセットアップする
完了定義: `npm run dev` を実行したブラウザに「baccarat」という見出しだけが表示される。
状態: 済

## 2. バカラの点数計算ロジックを実装する(カード配列→合計を9で割った下1桁)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、点数計算のテストがPASSと表示される。
状態: 済

## 3. Playerの第三カードルール判定ロジックを実装する(合計0-5は引く、6-7は止める、8-9はナチュラル)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、Player判定(0-5/6-7/8-9)のテストがPASSと表示される。
状態: 済

## 4. Bankerの第三カードルール判定ロジックを実装する(Playerが引いていない場合)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、Playerが引いていない場合のBanker判定テストがPASSと表示される。
状態: 済

## 5. Bankerの第三カードルール判定ロジックを拡張する(Playerが第三カードを引いた場合の判定表)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、Playerの第三カード値ごとのBanker判定(代表的な組み合わせ)のテストがPASSと表示される。
状態: 済

## 6. Pair判定ロジックを実装する(Player/Bankerそれぞれの初手2枚が同ランクかどうか)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、Pair成立/不成立それぞれの代表的な手のテストがPASSと表示される。
状態: 済

## 7. 52枚のデッキ生成・シャッフル・初期4枚配布ロジックを実装する
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、毎回異なる52枚から重複なくPlayer2枚/Banker2枚が配られることを確認するテストがPASSと表示される。
状態: 済

## 8. 第三カード判定(#3,#4,#5)と組み合わせ、1ハンド分を配布し切るロジックを実装する
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、第三カードが必要な手・不要な手それぞれで正しい枚数が配られることを確認するテストがPASSと表示される。
状態: 済

## 9. 第三カード発生率が70〜80%になるよう再抽選するロジックを実装する
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、多数ハンドをシミュレートして第三カード発生率が70〜80%に収まることを確認するテストがPASSと表示される。
状態: 済

## 10. 勝敗判定ロジックを実装する(Player win / Banker win / Tie)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、代表的な手の勝敗判定テストがPASSと表示される。
状態: 済

## 11. 「次のハンドへ」ボタンを設置し、配布結果と勝敗をそのまま画面に表示する(回答ステップはまだ無く、結果を見るだけ)
完了定義: 「次のハンドへ」を押すごとに、画面のPlayer/Bankerのカードと勝敗表示がランダムに変わる。
状態: 済

## 12. ベット種別(Player/Banker/Tie/Player Pair/Banker Pairの5種)とベット額のランダム選択ロジックを実装し、画面にそのハンドのベット内容を表示する
完了定義: 「次のハンドへ」を押すごとに、画面のベット表示(例: 「今回のベット: Banker $50」)が変わる。
状態: 済

## 13. Player Draw/Standの回答UIを設置し、正解/不正解を即時表示する
完了定義: Playerの初手2枚が表示された状態でDraw/Standのボタンを押すと、画面に正解/不正解が即時表示される。
状態: 済

## 14. Banker Draw/Standの回答UIを設置し、正解/不正解を即時表示する
完了定義: Bankerの初手(および必要な場合はPlayerの3枚目)が表示された状態でDraw/Standのボタンを押すと、画面に正解/不正解が即時表示される。
状態: 済

## 15. 勝敗判定の回答UI(Player win/Banker win/Tieの3択)を設置し、正解/不正解を即時表示する
完了定義: 全カードが開いた状態で3択ボタンのいずれかを押すと、画面に正解/不正解が即時表示される。
状態: 済

## 16. 配当テーブル(Player 1倍/Banker 0.95倍/Tie 8倍)の計算ロジックを実装する
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、Player win/Banker win/Tieそれぞれの配当計算テストがPASSと表示される。
状態: 済

## 17. 配当テーブルにPlayer Pair/Banker Pairの配当倍率(11倍)を追加する
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、Player Pair/Banker Pairそれぞれの配当計算テストがPASSと表示される。
状態: 済

## 18. 正解の配当額に対する4択ダミー(誤答選択肢)生成ロジックを実装する(Player/Banker/Tie/Player Pair/Banker Pairいずれの配当額にも対応)
完了定義: 画面表示に変化はない。`npx vitest run` を実行した端末に、生成される4択に正解が1つだけ含まれ、残り3つが正解と異なることを確認するテストがPASSと表示される。
状態: 済

## 19. 配当計算の回答UI(4択)を設置し、正解/不正解を即時表示する
完了定義: 4択ボタンのいずれかを押すと、画面に正解/不正解が即時表示される。
状態: 済

## 20. 誤答してもゲーム進行に影響しないことを確認する(Player Draw/Stand・Banker Draw/Stand・勝敗判定・配当計算)
完了定義: いずれの回答ステップで意図的に誤答を繰り返しても、その後配られるカード・最終的な勝敗表示・配当額は常に実際のカードの内容と一致し、変化しない(回答は正誤判定にのみ使われ、ゲーム進行には影響しない)。
状態: 済

## MVP2(今日やる分)

持ち帰り分(#21〜#41)のうち、以下の条件を両方満たす#21〜#28を今日中に実装する。
- 誤答時に、画面上で正解とその根拠(適用したルール・条件)がわかる
- 各回答項目とゲーム全体の正答率が、画面のスコアパネルで確認できる

## 21. Player Draw/Stand回答ステップに、不正解時の正解値と根拠(適用した引き足しルール)の一行表示を追加する
完了定義: Player Draw/Standで意図的に誤答すると、画面に正解(Draw/Stand)とその根拠(点数条件)が一行で表示される。
状態: 済

## 22. Banker Draw/Stand回答ステップに同様の不正解時表示を追加する
完了定義: Banker Draw/Standで意図的に誤答すると、画面に正解とその根拠(点数条件、Player第三カードの有無)が一行で表示される。
状態: 済

## 23. 勝敗判定の回答ステップに同様の不正解時表示を追加する
完了定義: 勝敗判定で意図的に誤答すると、画面に正解(Player win/Banker win/Tie)とその根拠(点数比較)が一行で表示される。
状態: 済

## 24. 配当計算の回答ステップに同様の不正解時表示を追加する
完了定義: 配当計算で意図的に誤答すると、画面に正解の配当額とその根拠(倍率)が一行で表示される。
状態: 済

## 25. スコアパネルの土台(5項目、初期値0%)を画面に追加する
完了定義: 画面にスコアパネルが表示され、Player Draw/Stand・Banker Draw/Stand・勝敗判定・配当計算・全体の5項目が0%で表示される。
状態: 済

## 26. Player Draw/StandとBanker Draw/Standの回答結果をスコアパネルに反映する
完了定義: Player Draw/StandまたはBanker Draw/Standに回答するたびに、該当項目の正答率がスコアパネルに反映される。
状態: 済

## 27. 勝敗判定と配当計算の回答結果、および全体正答率をスコアパネルに反映する
完了定義: 勝敗判定または配当計算に回答するたびに、該当項目と全体の正答率がスコアパネルに反映される。
状態: 済

## 28. スコアの手動リセットボタン(確認ダイアログ付き)を追加する
完了定義: リセットボタンを押すと確認ダイアログが表示され、OKを押すとスコアパネルの数値がすべて0に戻る。
状態: 済

## MVP3(明日やる分)

持ち帰り分(#29〜#41)のうち、以下の条件を両方満たす#39〜#41を明日中に実装する。
- ローカルビルド(`npm run build && npm run preview`)で正しいbaseパスからアプリが動作することを確認できる
- GitHub Pagesの公開URLでアプリが実際に開ける(そのためのビルド・デプロイが自動実行される)

## 39. Viteのbaseパスをリポジトリ名に合わせて設定し、ローカルビルドで動作確認する
完了定義: `npm run build && npm run preview` したブラウザで、`/baccarat/` 相当のパスからアプリが正しく表示される。
状態: 済

## 40. GitHub Actionsのビルド・デプロイworkflowを作成し、pushで自動実行されるようにする
完了定義: GitHubにpushすると、Actionsタブでワークフローが実行され、成功(緑のチェックマーク)が表示される。
状態: 済

## 41. GitHub Pagesの公開設定を行い、公開URLでアプリが表示されることを確認する
完了定義: `https://<user>.github.io/baccarat/` にアクセスするとアプリが表示される。
状態: 済

## 持ち帰る分

MVPの条件は満たさないが、スコープ内で後回しにする#29〜#38。各タスクに持ち帰る理由を追加している。

## 29. 第三カードルール表(カンニングシート)をトグルで表示/非表示できるようにする
完了定義: トグルボタンを押すとルール表が画面に表示され、もう一度押すと隠れる。
状態: 済
持ち帰る理由: 参照用の補助機能であり、判断練習の体験自体には必須ではないため。

## 30. ベット受付〜配布の自動演出を実装する(最初の回答ステップの手前で自動進行を止める)
完了定義: 「次のハンドへ」を押すと、ベット受付→カード配布の画面表示が自動で切り替わり、最初の回答が必要なステップの直前で自動進行が止まる。
状態: 済
持ち帰る理由: ステップ間の表示切り替えは演出無しの簡易な形でMVPとして動作するため、自動演出による磨き込みは次回。

## 31. 回答ステップ間の自動進行を実装する(1つ答えると次の回答ステップまで自動で進む)
完了定義: 1つの回答ステップに答えると、画面が自動で次のステップの表示に切り替わり、次に回答が必要な箇所でまた止まる。
状態: 済
持ち帰る理由: #30と同じ理由。

## 32. 全回答完了後の支払い演出を自動進行にする
完了定義: 最後の回答後、画面が自動で支払い結果(勝敗・配当表示)まで進み、「次のハンドへ」ボタンが再度押せる状態になる。
状態: 済
持ち帰る理由: #30と同じ理由。

## 33. ゲームエリア(カード表示・勝敗表示)にCSS Modulesで緑基調のテーブル風スタイルを適用する
完了定義: Player/Bankerのカード表示エリアが緑基調のテーブル風デザインで表示される。
状態: 済
持ち帰る理由: 機能が動く状態を優先し、見た目の装飾は次回。

## 34. 回答UI(ボタン・スコアパネル・カンニングシート)にスタイルを適用する
完了定義: 回答ボタン・スコアパネル・カンニングシートが#33と統一感のあるデザインで表示される。
状態: 済
持ち帰る理由: #33と同じ理由。

## 35. レスポンシブレイアウトを適用する
完了定義: ブラウザ幅をスマホ幅程度まで狭めてもレイアウトが崩れずに表示される。
状態: 済
持ち帰る理由: PC幅で機能が一通り確認できればMVPの目的は達成できるため、モバイル対応は次回。

## 36. 全ての回答ボタン・トグル・リセットボタンをTabキーで操作できるようにする
完了定義: Tabキーだけで全ての回答ボタン・トグル・リセットボタンに順番にフォーカスが移動する。
状態: 済(すべて標準のbutton要素のため追加実装なしで確認できた)
持ち帰る理由: マウス操作で全機能を確認できればMVPの目的は達成できるため、キーボード対応は次回。

## 37. フォーカス位置が視覚的にわかるようにフォーカスリングのスタイルを追加する
完了定義: Tabで要素を移動すると、現在フォーカスされている要素が画面上ではっきり分かる見た目になる。
状態: 済
持ち帰る理由: #36と同じ理由。

## 38. ARIA属性とカラーコントラストを調整する
完了定義: ブラウザの開発者ツールのアクセシビリティチェック(または類似ツール)で、主要な操作要素にラベルがあり、文字色と背景のコントラストが基準を満たすことを確認できる。
状態: 済
持ち帰る理由: #36と同じ理由。

## 今回やらないこと

- ログイン・成績保存・研修担当者向け閲覧機能 — バックエンドを持たない個人練習用ツールに限定するため
- シュー残数・リシャッフル管理などの複数デッキ運用 — 1ハンド完結・毎回新規配布という前提でスコープ外のため
- チップのドラッグ&ドロップ操作やカード配布そのものの手技練習 — 判断・計算スキルの練習に対象を絞るため
- リッチなカジノテーブル風ビジュアル・アニメーション — 学習効果に直結しない装飾コストを避けるため
- Player Pair / Banker Pair以外のサイドベット(Perfect Pair、Big/Smallなど) — v1で扱うベット種別を主要3種+Pair2種に限定するため
- 日本語以外のUI言語対応 — 対象ユーザーが日本語話者の研修生であるため
